# Phoenix Append Text

Appends a fixed string to the end of `text`.

## Inputs

- `text` — the input text.
- `append` — text appended to the end. Multiline, so you can start it with a literal newline if you want a gap before it.

## Output

- `text` — `text + append`, concatenated with no separator inserted automatically.

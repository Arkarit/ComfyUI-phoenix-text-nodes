# Phoenix Prepend Text

Prepends a fixed string to the start of `text`.

## Inputs

- `text` — the input text.
- `prepend` — text prepended to the start. Multiline, so you can end it with a literal newline if you want a gap after it.

## Output

- `text` — `prepend + text`, concatenated with no separator inserted automatically.
